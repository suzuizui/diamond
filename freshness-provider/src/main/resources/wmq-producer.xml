<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:wmq="http://www.wormpex.com/schema/wmq"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.wormpex.com/schema/wmq
       http://www.wormpex.com/schema/wmq.xsd">

  <!--   <wmq:consumer connection-factory="shareFactory">
        <wmq:listener topic="${subject.wormpex.cvs.order.core.create}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="orderCreatedListener"/>
        <wmq:listener topic="${subject.wormpex.cvs.order.core.pay.success}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="paySuccessListener"/>
        <wmq:listener topic="${subject.wormpex.cvs.order.core.sorted}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="sortedListener"/>
        <wmq:listener topic="${subject.wormpex.cvs.order.core.checking}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="checkingListener"/>
        <wmq:listener topic="${subject.wormpex.cvs.cart.change}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="cartOrderChangeListener"/>
        <wmq:listener topic="${subject.wormpex.cvs.order.core.takeaway.delivering}"
                      group="${group.wormpex.cvs.order.core}"
                      ref="takeawayDeliveringListener"/>
    </wmq:consumer> -->
</beans>